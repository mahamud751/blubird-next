"use client";
import { FormEvent,useState } from "react";import Link from "next/link";import { useRouter } from "next/navigation";import { StoreHeader } from "@/components/StoreHeader";import { api } from "@/lib/api";import type{Customer}from "@/lib/types";
export default function LoginPage(){const router=useRouter();const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const data=new FormData(e.currentTarget);try{const customers=await api<Customer[]>("/api/v1/customers");const found=customers.find(c=>c.email.toLowerCase()===String(data.get("email")).trim().toLowerCase());if(!found)throw new Error("We couldn't find an account with that email.");localStorage.setItem("blubird-customer",String(found.id));router.push("/orders");}catch(err){setError((err as Error).message);setBusy(false)}}return <><StoreHeader/><main className="auth-page"><section className="auth-promo"><div className="brand-mark">B<span/></div><p className="section-kicker">Welcome back</p><h1>Everything you love, all in one place.</h1><p>Track orders, reorder favorites, and get personalized help from your Blubird account.</p><div><span>✓ Fast, secure checkout</span><span>✓ Live order tracking</span><span>✓ Personal shopping support</span></div></section><form onSubmit={submit} className="auth-card"><p className="section-kicker">Your account</p><h2>Sign in to Blubird</h2><p>Enter the email associated with a demo customer.</p><label>Email address<input name="email" type="email" required placeholder="you@example.com" autoComplete="email"/></label><label>Password<input name="password" type="password" required placeholder="••••••••" autoComplete="current-password"/></label><div className="form-meta"><label><input type="checkbox"/> Remember me</label><a href="#">Forgot password?</a></div><button className="btn-primary" disabled={busy}>{busy?"Signing in…":"Sign in →"}</button>{error?<p className="form-error">{error}</p>:null}<p className="auth-switch">New to Blubird? <Link href="/register">Create an account</Link></p><small>Demo note: passwords are not validated because the provided backend does not store credentials.</small></form></main></>}
