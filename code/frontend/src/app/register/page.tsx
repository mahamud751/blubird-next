"use client";
import { FormEvent,useState } from "react";import Link from "next/link";import { useRouter } from "next/navigation";import { StoreHeader } from "@/components/StoreHeader";import { api } from "@/lib/api";import type{Customer}from "@/lib/types";
export default function RegisterPage(){const router=useRouter();const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const data=new FormData(e.currentTarget);if(data.get("password")!==data.get("confirm")){setError("Passwords do not match.");setBusy(false);return}try{const customer=await api<Customer>("/api/v1/customers",{method:"POST",body:JSON.stringify({name:data.get("name"),email:data.get("email")})});localStorage.setItem("blubird-customer",String(customer.id));router.push("/");}catch(err){setError((err as Error).message);setBusy(false)}}return <><StoreHeader/><main className="auth-page"><section className="auth-promo register-promo"><div className="brand-mark">B<span/></div><p className="section-kicker">Join Blubird</p><h1>A better way to shop starts here.</h1><p>Create your free account to save your details, place orders, and keep every purchase organized.</p><div><span>✓ No membership fees</span><span>✓ Simple, transparent pricing</span><span>✓ Easy order management</span></div></section><form onSubmit={submit} className="auth-card"><p className="section-kicker">Get started</p><h2>Create your account</h2><p>It only takes a minute.</p><label>Full name<input name="name" required placeholder="Your full name" autoComplete="name"/></label><label>Email address<input name="email" type="email" required placeholder="you@example.com" autoComplete="email"/></label><div className="form-grid"><label>Password<input name="password" type="password" minLength={6} required placeholder="6+ characters"/></label><label>Confirm password<input name="confirm" type="password" minLength={6} required placeholder="Repeat password"/></label></div><label className="terms"><input type="checkbox" required/> I agree to the terms and privacy policy.</label><button className="btn-primary" disabled={busy}>{busy?"Creating account…":"Create account →"}</button>{error?<p className="form-error">{error}</p>:null}<p className="auth-switch">Already have an account? <Link href="/login">Sign in</Link></p></form></main></>}
